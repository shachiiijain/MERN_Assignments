let colors = ['red', 'blue', 'green', 'yellow', 'blue'];
let index = colors.indexOf('green');
console.log('Index of green:', index);
let firstBlueIndex = colors.indexOf('blue');
console.log('Index of first blue:', firstBlueIndex);
let indexNotFound = colors.indexOf('purple');
console.log('Index of purple:', indexNotFound); 